- Use monthsInterval, token, username in mock objects to verify they are passed correctly
- Configurable date format, don't force h2, h3
- Configurable contributions sorting
- Configurable ignorable repos
